<header class="ui fixed inverted menu">

    <a href="#" class="launch item">
        <i class="content icon"></i> Menu
    </a>

    <a href="{{ url('/') }}" target="_blank" class="title item">
        <i class="icon home"></i> My Site
    </a>

    <div class="right menu">
        <div class="ui user dropdown item">

            {{ auth_user().display_name ?: auth_user().username }}

            <img src="https://gravatar.com/avatar/{{ md5(auth_user().email) }}" height="24">

            <div class="menu wide">

                <div class="header">
                    <img src="https://gravatar.com/avatar/{{ md5(auth_user().email) }}" class="ui medium circular image">
                    <br>
                    <b>{{ auth_user().email }}</b>
                </div>

                <div class="item">
                    <i class="icon user"></i>
                    Edit Profile
                </div>

                <a href="{{ url('admin/logout') }}" class="item">
                    <i class="icon sign out"></i>
                    Logout
                </a>
            </div>

        </div>
    </div>

</header>